framework:
    rate_limiter:
        invitation:
            policy: 'sliding_window'
            limit: 30
            interval: '15 minutes'

when@test:
    framework:
        rate_limiter:
            invitation:
                policy: 'sliding_window'
                limit: 10000
                interval: '15 minutes'
